import 'package:flutter/material.dart';

class FurnitureDetailScreen extends StatelessWidget {
  const FurnitureDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Furniture Detail screen'),
      ),
    );
  }
}